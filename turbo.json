{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "storybook-static/**",
        "dist/**"
      ]
    },
    "test": {},
    "lint": {
      "outputLogs": "errors-only"
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
