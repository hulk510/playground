{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "test": {},
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check": {
      "dependsOn": ["test", "lint"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
