---
import Posts from "@layouts/Posts.astro";

import { getPayloadCollection } from "../../services/api";
import type { Blog } from "../../services/api/types";

const blogs = await getPayloadCollection<Blog>(
  "http://localhost:8888/api/blogs"
);
---

<Posts posts={blogs} />
